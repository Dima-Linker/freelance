{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "contract:emit": "prisma contract emit"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0"
  }
}
